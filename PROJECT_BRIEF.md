# 项目说明

## 项目名称
默写不费妈

## 项目目标
这是一个微信小程序 MVP，解决孩子做语文或英语默写时，需要家长人工读题、陪练、批改，过程费时费力的问题。

目标是让孩子可以自己完成默写流程：
1. AI 自动报默
2. 孩子自己书写
3. 写完拍照上传
4. 系统自动批改
5. 自动记录错题
6. 自动生成复习任务

## 目标用户
小学阶段孩子家长。

优先服务的使用场景：
- 语文词语默写
- 英语单词默写
- 英语短句默写

## MVP 只做 4 个功能
1. AI 报默
2. 拍照批改
3. 错题本
4. 历史复习

## 明确不做的内容
当前 MVP 阶段不要加入以下功能：
- 社区
- 排行榜
- 商城
- 课程
- 积分系统
- 分享裂变
- 聊天
- 广告
- 数学练习
- 作文批改
- 长篇课文整页精批
- 复杂多角色系统

## 设计稿说明
当前项目目录中的 `design-ui.html` 是 UI 设计稿，只用于视觉与交互参考。

非常重要：
- `design-ui.html` 不是最终业务代码
- 不允许直接把 HTML 当成最终实现
- 必须将其 **1:1 复刻为微信小程序原生界面**
- 这里的 1:1 复刻，指的是尽量保持以下一致：
  - 页面布局
  - 视觉层级
  - 颜色风格
  - 圆角、阴影、间距
  - 卡片样式
  - 按钮位置
  - 主次信息关系
  - 交互节奏
  - 弹窗样式
  - 底部导航结构

如果 HTML 中某些实现方式不适合微信小程序原生环境，允许技术上替换实现方式，但**最终视觉效果必须尽量接近原稿**，不允许擅自重新设计 UI。

设计同步说明：
- 当前设计稿已包含显式学科切换入口
- 阶段 2 必须增加“学科 + 模式”两个显式选择控件
- 不允许自动推断输入内容属于哪种模式
- 后续实现时，设计稿中的学科切换入口不能为了实现方便被删除

## 技术要求
请默认使用以下技术方案：
- 微信小程序原生
- WXML / WXSS / JS
- 微信云开发
- 云函数
- 小程序原生页面与组件

限制：
- 不要使用 WebView 套壳
- 不要直接复用 HTML 运行
- 不要引入复杂框架，除非后续明确要求
- 代码尽量简单直白，方便维护和理解

## 开发原则
1. 先 UI，后功能
2. 先跑通 MVP 闭环，再做优化
3. 每次只完成一个阶段，不要一次性生成完整大项目
4. 不要擅自增加功能
5. 不要擅自改变 UI 风格
6. 所有新增或修改代码必须标明文件路径
7. 如果某个 AI 能力暂时无法真实接入，先用 mock + TODO 占位
8. 保持代码清晰，适合非专业开发者理解

## 当前开发顺序
请按以下顺序推进：

### 阶段 1
先读取 `design-ui.html`，分析并输出：
- 页面拆分方案
- 组件拆分方案
- 项目目录结构
- 哪些 HTML 设计可以直接映射到小程序
- 哪些部分需要改用小程序原生方式实现

然后开始做：
- 微信小程序项目骨架
- 页面骨架
- UI 1:1 复刻
- 基础页面跳转
- 弹窗开关
- 底部导航切换
- 占位数据展示

本阶段不要接：
- OCR
- AI 批改
- 云数据库
- 云函数
- 真实 TTS
- 错题存储
- 复习逻辑

### 阶段 2
接入“AI 报默”最小可用功能：
- 粘贴文本创建任务
- 学科选择：语文 / 英语
- 模式选择：词语 / 单词 / 短句
- 自动拆分默写条目
- 创建任务前，用户必须显式确认当前学科和模式
- 控件要求：
  - 学科控件必须显式展示：语文、英语
  - 模式控件必须显式展示：词语、单词、短句
  - 不允许自动推断输入内容属于哪种模式
- 当前 MVP 允许的学科 + 模式组合：
  - 语文：词语
  - 英语：单词、短句
- 当前不支持的组合处理方式：
  - 在 UI 中保留展示
  - 采用禁用态，不隐藏、不自动切换
- 条目拆分规则：
  - 词语 / 单词：按空格或换行拆分
  - 短句：按每行一条拆分，不按空格拆
- `拍教材 / AI帮你读` 入口在阶段 1 / 2 仅做 UI 占位，不接真实拍照创建任务流程
- `AI 助手` 在阶段 2 可使用 mock：
  - “一键生成易错词库”可直接生成 mock 任务内容
  - “薄弱知识点分析”使用底部结果弹窗展示 mock 内容
  - “薄弱知识点分析”的 mock 结果至少展示 2-3 个问题点、简短说明和建议动作
- 听写进行页支持：
  - 播放当前题
  - 上一题
  - 下一题
  - 进度显示
  - 自动播放开关
  - 播放间隔设置
  - 偷看答案开关

本阶段先允许 mock。

### 阶段 3
接入“拍照批改”最小可用流程：
- 上传默写照片
- OCR 接口层
- 内容切分接口层
- 答案比对接口层
- 返回批改结果
- 展示正确数、错误数、逐题明细
- 支持将错误项加入错题本

本阶段允许 mock，但接口结构必须清晰，方便后续替换真实服务。

### 阶段 4
接入“错题本”：
- 错题自动写入 wrong_book
- 错题列表展示
- 按学科筛选
- 按日期查看
- 显示错误次数
- 支持再次生成默写任务

### 阶段 5
接入“历史复习”：
- 基于错题生成 D1 / D3 / D7 复习任务
- 复习页展示待复习 / 已完成任务
- 点击进入再次默写

## MVP 页面范围
建议至少包含以下页面或视图：
1. 首页
2. 创建任务相关弹窗或页面
3. 学科选择控件
4. 模式选择控件
5. 听写进行页
6. AI 分析结果底部弹窗
7. 提交批改页
8. 批改结果页
9. 错题本页
10. 历史复习页
11. AI 助手弹窗

## 数据结构建议
后续至少会涉及以下核心实体：
- users
- dictation_tasks
- dictation_items
- submissions
- correction_results
- wrong_book
- review_tasks

当前阶段如果未接真实数据库，可先定义数据结构与 mock 数据。

## 关键约束
请始终遵守：
1. 优先保证 `design-ui.html` 的 1:1 复刻
2. 不要把“风格接近”当成“1:1 复刻”
3. 当前阶段只做当前阶段要求
4. 不要提前开发后续功能
5. 不要引入无关依赖
6. 不要重构未要求的模块
7. 所有新增/修改代码必须标明文件路径
8. 如果存在不确定实现，先用 mock + TODO 占位
9. 阶段 2 不允许自动推断学科或模式
10. 当前不支持的学科 + 模式组合必须显示但禁用，不能隐藏或自动切换
11. 不要为了实现方便删除设计稿中已出现的学科切换入口

## 输出要求
每一轮输出请严格包含：
1. 本阶段目标
2. 新增/修改文件路径
3. 关键实现说明
4. 完整代码
5. 如何运行/测试
6. 下一阶段建议
