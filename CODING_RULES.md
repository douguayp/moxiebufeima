# CODING_RULES

## 目标
本项目是“默写不费妈”微信小程序 MVP。

开发时必须始终遵守以下规则：
- 优先保证 UI 1:1 复刻
- 再逐步接入功能
- 每次只完成当前阶段任务
- 不要擅自发挥

---

## 一、最重要规则：design-ui.html 必须 1:1 复刻

`design-ui.html` 是本项目唯一的 UI 设计依据。

### 强制要求
1. 必须严格参考 `design-ui.html` 进行微信小程序原生实现。
2. 不允许擅自修改以下内容：
   - 页面布局
   - 模块顺序
   - 视觉层级
   - 主色调
   - 卡片样式
   - 圆角
   - 阴影
   - 间距
   - 按钮位置
   - 弹窗样式
   - 底部导航结构
   - 播放器区域布局
3. 不允许把“风格接近”当成“1:1 复刻”。
4. 如果 HTML 的实现方式不适用于微信小程序，允许技术上改写，但最终视觉效果必须尽量接近原稿。
5. 如果某个部分无法完全等效实现，必须：
   - 明确指出原因
   - 提供最接近的替代方案
   - 不要擅自重新设计

---

## 二、技术实现规则

### 必须使用
- 微信小程序原生
- WXML
- WXSS
- JavaScript
- 微信云开发
- 云函数

### 禁止使用
- WebView 套壳
- 直接运行 HTML 页面作为最终实现
- 未经说明引入大型框架
- 无必要的复杂状态管理
- 无必要的第三方 UI 框架

### 代码原则
1. 代码尽量简单直白。
2. 适合非专业开发者理解和维护。
3. 不要过度抽象。
4. 不要为了“架构好看”牺牲可读性。
5. 公共逻辑可以封装，但不要过度拆分。

---

## 三、开发顺序规则

必须严格按阶段推进，不允许跳阶段。

### 阶段 1：UI 复刻
只做：
- 小程序项目骨架
- 页面结构
- 页面样式
- 基础页面跳转
- 底部导航切换
- 弹窗开关
- 占位数据展示

不要做：
- OCR
- AI 批改
- 云数据库真实写入
- 云函数真实业务逻辑
- 真实 TTS
- 错题存储
- 复习任务生成

### 阶段 2：AI 报默
只做：
- 粘贴文本创建任务
- 学科选择
- 模式选择
- 文本拆分
- 听写进行页基础逻辑
- 播放控制
- 自动播放
- 间隔设置
- 偷看答案开关

### 阶段 3：拍照批改
只做：
- 上传照片
- OCR 接口层
- 内容切分接口层
- 答案比对接口层
- 批改结果展示
- 错误项加入错题本

### 阶段 4：错题本
只做：
- 错题写入
- 错题列表
- 学科筛选
- 日期查看
- 错误次数展示
- 再次生成默写任务

### 阶段 5：历史复习
只做：
- D1 / D3 / D7 复习任务生成
- 复习页展示
- 待复习 / 已完成状态
- 点击再次进入默写

---

## 四、功能范围规则

### MVP 只允许做这 4 个功能
1. AI 报默
2. 拍照批改
3. 错题本
4. 历史复习

### 明确禁止新增
- 社区
- 排行榜
- 商城
- 课程
- 积分系统
- 分享裂变
- 聊天
- 广告
- 数学功能
- 作文批改
- 长篇课文复杂精批
- 多角色后台系统
- 与 MVP 无关的扩展功能

---

## 五、文件修改规则

1. 所有新增或修改代码都必须明确写出文件路径。
2. 修改现有文件时，必须说明：
   - 修改了哪个文件
   - 修改了哪部分逻辑
   - 修改目的是什么
3. 不允许静默修改大量无关文件。
4. 不允许未经说明重构已有模块。
5. 每次只改当前阶段需要的文件。

---

## 六、输出规则

每一轮输出必须严格包含以下 6 项：

1. 本阶段目标
2. 新增/修改文件路径
3. 关键实现说明
4. 完整代码
5. 如何运行/测试
6. 下一阶段建议

### 禁止的输出方式
- 只讲思路，不给代码
- 一次性输出整个大项目全部代码
- 不写文件路径
- 不说明修改点
- 输出大量与当前阶段无关的内容

---

## 七、Mock 规则

如果某个真实能力暂时无法接入，必须先使用 mock，并标明 `TODO`。

适用于：
- TTS
- OCR
- AI 批改
- 云函数接口
- 图片识别结果
- 复习任务调度

### Mock 要求
1. mock 结构必须接近真实接口结构。
2. 不要把 mock 数据直接硬编码在页面模板里。
3. mock 逻辑要单独封装，后续便于替换真实服务。
4. 必须预留接口层，不要把 mock 写死在业务页面中。

---

## 八、UI 修改限制

除非我明确要求，否则：
1. 不要修改颜色方案。
2. 不要修改卡片布局。
3. 不要修改页面信息层级。
4. 不要新增设计元素。
5. 不要删除设计稿中的关键视觉元素。
6. 不要重新设计首页。
7. 不要随意改动底部导航。
8. 不要改变听写进行页播放器区域的结构。

---

## 九、页面与组件规则

1. 页面和组件拆分要合理。
2. 不要把整个项目塞进一个页面文件。
3. 允许提取公共样式、公共组件、公共工具函数。
4. 但不要过度组件化，避免难以理解。
5. 公共设计 token 应统一管理，例如：
   - 颜色
   - 圆角
   - 阴影
   - 字号
   - 间距

---

## 十、数据结构规则

后续涉及的数据结构至少包括：
- users
- dictation_tasks
- dictation_items
- submissions
- correction_results
- wrong_book
- review_tasks

即使当前阶段尚未接入真实数据库，也应先设计好字段结构，并保持字段命名清晰一致。

---

## 十一、代码风格规则

1. 变量命名清晰。
2. 函数职责单一。
3. 注释只写必要内容。
4. 不要炫技。
5. 不要写难维护的“聪明代码”。
6. 优先可读性。
7. 优先小步迭代。

---

## 十二、遇到不确定情况时的处理规则

如果遇到不确定情况，按以下顺序处理：
1. 优先保持 UI 不变
2. 优先保持当前阶段边界不变
3. 优先用 mock 占位
4. 在代码中写清楚 `TODO`
5. 明确说明存在的不确定点

不要擅自脑补需求，不要自行扩展范围。

---

## 十三、最终原则

本项目不是让你自由发挥做一个学习产品。

本项目的要求是：
- 严格按照 `design-ui.html` 进行 1:1 复刻
- 严格按照 MVP 范围逐步开发
- 严格按照阶段推进
- 严格控制输出和改动范围

如果你需要在“设计一致性”和“个人优化想法”之间做选择，永远优先选择：
**设计一致性。**
